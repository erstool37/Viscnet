# Viscnet Rebuild Reproduction

This runbook has been merged into `distillation.md`.

Use `distillation.md` as the canonical handoff for:

- rebuild training order
- checker/analyzer workflow
- current experiment results
- 21-window inference
- raw-video config generation
- GitHub and dataset-safety state

Keep this file only as a compatibility pointer for agents or scripts that expect `REBUILD_REPRODUCTION.md` to exist.
